package internship.paypal.online;

import java.util.ArrayList;
import java.util.HashMap;
import java.util.Scanner;

/**
 * Created by Cc on 2017/4/13.
 */

public class WiredWire {
//    private static int n, ceiling;
//    private static HashMap<Integer, Integer> graph;
//    public static void main(String[] args) {
//        Scanner s = new Scanner(System.in);
//        while(s.hasNext()) {
//            ceiling = Integer.MAX_VALUE;
//            n = s.nextInt();
//            graph = new HashMap<>();
//            for(int i = 0; i < n - 1; i ++)
//                graph.put(s.nextInt(), s.nextInt());
//            ArrayList<Integer> keys = new ArrayList<>(), ans = new ArrayList<>();
//            for(Integer i : graph.keySet())
//                if(!graph.containsKey(graph.get(i)))
//                    keys.add(i);
//            solve(keys, 0, 0, ans);
//        }
//        s.close();
//    }
//    private static void solve(ArrayList keys, int start, int cnt, ArrayList ans) {
//        for(int i = 1; i <= n; i ++)
//            if(isArrived(i))
//                ceiling = ceiling < cnt ? ceiling : cnt;
//        for(int i = start; i < keys.size(); i ++) {
//        }
//    }
//    private static boolean isArrived(int node) {
//    }
}